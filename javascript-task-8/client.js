/* eslint-disable no-console */
'use strict';

module.exports.isStar = true;

const { execute } = require('./client-core');

module.exports.isStar = true;

execute()
    .then(console.log)
    .catch(console.error);

